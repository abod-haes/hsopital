import { vw } from "@/utils/vwFont";

const scalingPoints = {
  //size of font
  "--fs-xs": vw(2.5),
  "--fs-sm": vw(3),
  "--fs-base": vw(3.6),
  "--fs-lg": vw(4),
  "--fs-xl": vw(5),
  "--fs-2xl": vw(6),

  //spaces like margin and padding:
  // "--space-xs": vw(2.5),
  // "--space-sm": vw(3),
  // "--space-base": vw(4),
  // "--space-lg": vw(5.5),
  // "--space-xl": vw(7),
};

export default scalingPoints;
